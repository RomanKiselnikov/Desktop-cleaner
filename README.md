# 📁 Desktop Cleaner - Организатор файлов

**Автоматический организатор файлов на Python**, который сортирует файлы по папкам в зависимости от их типа.

## ✨ Основные возможности

- 📂 **Сканирование папки** - анализ содержимого выбранной директории
- 🏷️ **Автоматическая классификация** - определение типа файла по расширению
- 📁 **Создание папок категорий** - автоматическое создание структурированных папок
- 🚚 **Перемещение файлов** - быстрая сортировка файлов по соответствующим папкам
- ⚠️ **Обработка ошибок** - пропускает системные файлы и обрабатывает исключения

## 📋 Поддерживаемые категории файлов

| Категория | Расширения |
|-----------|------------|
| **Изображения** | .jpg, .jpeg, .png, .gif, .bmp |
| **Документы** | .pdf, .doc, .docx, .txt, .rtf |
| **Видео** | .mp4, .avi, .mov, .mkv |
| **Музыка** | .mp3, .wav, .flac, .aac |
| **Архивы** | .zip, .rar, .7z, .tar |
| **Программы** | .exe, .msi, .bat, .sh |
| **Другое** | Все остальные файлы |

## 🚀 Быстрый старт

### Требования
- Python 3.8 или выше
- Только стандартные библиотеки (Pathlib, Shutil)

### Установка и запуск
```bash
# 1. Скачайте скрипт
git clone https://github.com/ваш-репозиторий/desktop-cleaner.git
cd desktop-cleaner

# 2. Запустите скрипт
python Desktop_cleaner.py

# Или прямо с указанием пути:
python Desktop_cleaner.py "C:\Users\Имя\Desktop"
```

### Пример использования
```python
# Запуск с указанием папки
python Desktop_cleaner.py "C:\Users\Иван\Downloads"

# Или просто запустите и введите путь при запросе
python Desktop_cleaner.py
```

## 🎯 Как работает

### 1. **Сканирование** (`scanner()` функция)
- Запрашивает у пользователя путь к папке (или использует текущую)
- Проверяет существование папки и корректность пути
- Анализирует все файлы, определяя их тип по расширению
- **Автоматически пропускает** сам скрипт `Desktop_cleaner.py`

### 2. **Создание папок** (`create_category_folders()` функция)
- Создает папки для каждой категории, в которой есть файлы
- Использует безопасное создание (`exist_ok=True`)
- Отображает прогресс создания папок

### 3. **Перемещение файлов** (`move_with_rename()` функция)
- Использует быстрый метод `.rename()` для перемещения в пределах одного диска
- Автоматически переключается на `shutil.move()` для перемещения между дисками
- Отображает подробный отчет о перемещении

## 📁 Структура проекта

```
Desktop_cleaner.py        # Основной скрипт
README.md                # Этот файл
```

## 🛠️ Настройка

### Добавление новых категорий
Для добавления поддержки новых типов файлов отредактируйте словарь `categories` в функции `scanner()`:

```python
categories = { 
    "Новая категория": [".новое_расширение", ".другое_расширение"],
    # ... существующие категории
}
```

### Исключение файлов
Скрипт автоматически пропускает:
- Сам себя (`Desktop_cleaner.py`)
- Файлы без расширений

Для исключения других файлов добавьте проверку в цикле сканирования:
```python
if file.name in ["desktop.ini", "Thumbs.db"]:
    continue
```

## ⚠️ Меры предосторожности

- **Скрипт не удаляет** исходные файлы, только перемещает их
- **Сохраняется исходная структура имен** файлов
- **При конфликте имен** (файл уже существует в папке назначения) будет ошибка
- **Рекомендуется сначала протестировать** на копии важных данных

## 🔧 Технические детали

### Используемые технологии
- **Pathlib** - кросс-платформенная работа с путями
- **Shutil** - перемещение файлов между дисками
- **Встроенные методы Python** - максимальная производительность

### Особенности реализации
- **Быстрое перемещение** - использует `.rename()` для файлов в пределах одного диска
- **Автоматическое переключение** - на `shutil.move()` при необходимости
- **Подробное логирование** - каждый шаг отображается в консоли
- **Обработка исключений** - ошибки прав доступа, отсутствия файлов и т.д.

## 📊 Пример вывода

```
📂 Сканируем: C:\Users\Иван\Desktop
✅ Сканирование завершено! Всего файлов: 42

📁 Создаем папки для категорий...
✅ Изображения     → Images/
✅ Документы       → Documents/
✅ Архивы          → Archives/

🚚 Перемещение файлов:
✅ photo.jpg       → Изображения/
✅ report.pdf      → Документы/
✅ archive.zip     → Архивы/

✅ Готово! 42 файла отсортированы.
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 🆘 Поддержка

Если у вас возникли проблемы:
1. Убедитесь, что Python установлен корректно
2. Проверьте права доступа к целевой папке
3. Запустите скрипт от имени администратора (если нужно)

## 📈 Планы развития

- [ ] Добавить поддержку конфигурационного файла
- [ ] Реализовать отмену перемещения (undo)
- [ ] Добавить прогресс-бар для больших папок
- [ ] Поддержка рекурсивной обработки подпапок
- [ ] Экспорт отчета в JSON/CSV

---

**Создано с ❤️ для упорядочивания цифрового хаоса**
